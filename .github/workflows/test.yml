# vim: syntax=json
{
	"name": "Tests",
	"on": ["push", "pull_request"],
	"jobs": {
		"test": {
			"runs-on": "ubuntu-slim",
			"steps": [
				{"uses": "actions/checkout@v6"},
				{"uses": "actions/setup-node@v6"},
				{"run": "npm ci"},
				{"run": "npm test"}
			]
		}
	}
}